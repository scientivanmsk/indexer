# Ponder Indexer Environment Variables

# ========================================
# BACKEND URL (REQUIRED)
# ========================================
# URL backend API yang akan menerima webhook dari Ponder
# 
# Development (Local):
# BACKEND_URL=http://localhost:3300
#
# Production (Railway/Vercel/Render):
# BACKEND_URL=https://your-backend-url.up.railway.app
#
BACKEND_URL=http://localhost:3300

# ========================================
# PONDER SYNC API KEY (REQUIRED)
# ========================================
# API key untuk autentikasi webhook ke backend
# Harus SAMA dengan PONDER_SYNC_API_KEY di backend .env
#
PONDER_SYNC_API_KEY=ponder-sync-key

# ========================================
# RAILWAY DEPLOYMENT NOTES
# ========================================
# Saat deploy ke Railway:
# 1. Set environment variables di Railway dashboard
# 2. BACKEND_URL harus URL public backend (bukan localhost!)
# 3. PONDER_SYNC_API_KEY harus sama dengan backend
# 4. Restart service setelah update environment variables
